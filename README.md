# alv_loadingscreen
FiveM loading screen that supports local files or YouTube API. Has a media player which supports multiple music files and other UI features. Completely standalone requires no specific framework such as ESX, OX or QB. 
